---
title: "Implementation Pattern Validation Guide"
---
# Implementation Pattern Validation Guide

Run `python scripts/validate.py`. The validator checks the required directory contract, manifest schema, catalogue membership, profile and requirement references, artifact existence, fixture classification, behavioural references, scenarios, maturity evidence, and conformance limitations. Missing or malformed files produce named failures rather than stopping validation.
